//! Worker scheduled_jobs — TODO Bagian I Phase 3 (poll FOR UPDATE SKIP LOCKED,
//! tandai RUNNING di transaksi pendek, eksekusi di luar lock, dedupe_key,
//! stale-RUNNING recovery). Stub agar bin target valid.
fn main() {
    println!("mq-worker: belum diimplementasi (Bagian I Phase 3)");
}
